# Terraform working directories and provider binaries
**/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
crash.log
crash.*.log
.terraformrc
terraform.rc

# Lock files belong to root configurations, not reusable child modules.
.terraform.lock.hcl

# macOS
.DS_Store
